<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ad %>
    <%= form.text_field :ad %>
  </div>

  <div class="field">
    <%= form.label :soyad %>
    <%= form.text_field :soyad %>
  </div>

  <div class="field">
    <%= form.label :yas %>
    <%= form.number_field :yas %>
  </div>

  <div class="field">
    <%= form.label :yorum %>
    <%= form.text_area :yorum %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
